#include<iostream>
using namespace std;

int main(void){
    int length,bredth;
    cout<<"Enter the length and bredth respectively: ";
    cin>>length>>bredth;
    int area = length * bredth;
    cout<<"The area of the Rectangle is "<<area;
    return 0;
}